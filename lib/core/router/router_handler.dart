part of 'router.dart';

Widget _homeHandler(BuildContext context, GoRouterState state) =>
    const HomePage();

Widget _detailsHandler(BuildContext context, GoRouterState state) =>
    const DetailsPage();
